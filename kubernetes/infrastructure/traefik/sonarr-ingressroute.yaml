apiVersion: v1
kind: Service
metadata:
  name: sonarr
  namespace: default
spec:
  ports:
  - port: 8989
    protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: sonarr
  namespace: default
subsets:
- addresses:
  - ip: 192.168.1.213
  ports:
  - port: 8989
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sonarr-ingressroute
  namespace: default
spec:
  entryPoints:
    - websecure
  routes:
  - match: Host(`sonarr.mistyfumes.dev`)
    kind: Rule
    services:
    - name: sonarr
      port: 8989
      serversTransport: insecure-transport
  tls:
    secretName: prod-mistyfumes-tls
---
apiVersion: traefik.io/v1alpha1
kind: ServersTransport
metadata:
  name: insecure-transport
  namespace: default
spec:
  insecureSkipVerify: true