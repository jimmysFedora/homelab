apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-drawio.fullname" . }}
  labels:
    {{- include "my-drawio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-drawio.selectorLabels" . | nindent 4 }}
