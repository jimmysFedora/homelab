apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: prod-
namespace: default
resources:
  - ../../base
configMapGenerator:
  - name: actual-budget-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true