apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
configMapGenerator:
  - name: metube-values
    namespace: default
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true