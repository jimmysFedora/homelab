apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-it-tools.fullname" . }}
  labels:
    {{- include "my-it-tools.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-it-tools.selectorLabels" . | nindent 4 }}
