apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "termix-ssh.fullname" . }}-test-connection"
  labels:
    {{- include "termix-ssh.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "termix-ssh.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
