apiVersion: v1
kind: Service
metadata:
  name: {{ include "termix-ssh.fullname" . }}
  labels:
    {{- include "termix-ssh.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "termix-ssh.selectorLabels" . | nindent 4 }}
