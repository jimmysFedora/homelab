apiVersion: v1
kind: Service
metadata:
  name: truenas
  namespace: default
spec:
  ports:
  - port: 443
    protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: truenas
  namespace: default
subsets:
- addresses:
  - ip: 192.168.1.130
  ports:
  - port: 443
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: truenas-ingressroute
  namespace: default
spec:
  entryPoints:
    - websecure
  routes:
  - match: Host(`truenas.mistyfumes.dev`)
    kind: Rule
    services:
    - name: truenas
      port: 443
      scheme: https
      serversTransport: insecure-transport
  tls:
    secretName: prod-mistyfumes-tls
---
apiVersion: traefik.io/v1alpha1
kind: ServersTransport
metadata:
  name: insecure-transport
  namespace: default
spec:
  insecureSkipVerify: true