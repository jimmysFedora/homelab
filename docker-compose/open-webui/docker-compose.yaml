services:
  openwebui:
    image: ghcr.io/open-webui/open-webui:main
    ports:
      - "3150:8080"
    volumes:
      - /mnt/docker-nfs/volumes/open-webui-data:/app/backend/data
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]   