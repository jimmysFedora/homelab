apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: prod-
resources:
  - ../../base/drawio
configMapGenerator:
  - name: drawio-values
    namespace: default
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
patches:
  - patch: |-
      - op: add
        path: /spec/valuesFrom
        value:
          - kind: ConfigMap
            name: prod-drawio-values
            valuesKey: values.yaml
    target:
      kind: HelmRelease
      name: drawio