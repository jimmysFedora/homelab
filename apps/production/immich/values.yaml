persistence:
  photography:
    enabled: true
    type: persistentVolumeClaim
    existingClaim: octapool-photography-library-pvc
    globalMounts:
      - path: /mnt/octapool-photography
        readOnly: true

  images:
    enabled: true
    type: persistentVolumeClaim
    existingClaim: octapool-images-library-pvc
    globalMounts:
      - path: /mnt/octapool-images
        readOnly: true

controllers:
  main:
    containers:
      main:
        env:
          DB_HOSTNAME: immich-db
        envFrom:
          - secretRef:
              name: immich-secrets

immich:
  metrics:
    # Enabling this will create the service monitors needed to monitor immich with the prometheus operator
    enabled: false
  persistence:
    # Main data store for all photos shared between different components.
    library:
      # Automatically creating the library volume is not supported by this chart
      # You have to specify an existing PVC to use
      existingClaim: immich-library-pvc
  # configuration is immich-config.json converted to yaml
  # ref: https://immich.app/docs/install/config-file/
  #

valkey:
  enabled: true

server:
  enabled: true
  ingress:
    main:
      enabled: true
      className: traefik
      hosts:
        - host: immich.mistyfumes.dev
          paths:
            - path: "/"
              service:
                identifier: main
      tls:
        - secretName: prod-mistyfumes-tls
          hosts:
            - immich.mistyfumes.dev
