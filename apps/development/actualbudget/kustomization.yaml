apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: dev-
resources:
  - ../../base
configMapGenerator:
  - name: actual-budget-values
    namespace: default
    files:
      - values.yaml=dev-values.yaml
    options:
      disableNameSuffixHash: true
patches:
  - patch: |-
      - op: add
        path: /spec/valuesFrom
        value:
          - kind: ConfigMap
            name: dev-actual-budget-values
            valuesKey: values.yaml
    target:
      kind: HelmRelease
      name: actual-budget