app-template:
  controllers:
    main:
      containers:
        main:
          image:
            repository: jgraph/drawio
            tag: "{{.Chart.AppVersion}}"
      strategy: Recreate

  ingress:
    main:
      className: traefik
      hosts:
      - host: drawio.dev.mistyfumes.dev
        paths:
        - path: /
          pathType: Prefix
          service:
            identifier: main
            port: http
      tls:
      - hosts:
        - drawio.dev.mistyfumes.dev
        secretName: dev-mistyfumes-tls

  service:
    main:
      controller: main
      ports:
        http:
          port: 8080